#include "main.h"

/**
 * _isupper - Check for uppercase letter
 * @c: the int we will use to argue the function
 * 
 * Return: 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z');
	{
		return (1);
	}
	else
	{
		return(0);
	}
}
